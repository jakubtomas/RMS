import { FormatMeetingPipe } from './format-meeting.pipe';

describe('FormatMeetingPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatMeetingPipe();
    expect(pipe).toBeTruthy();
  });
});
