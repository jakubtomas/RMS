import { OrderBusinessesPipe } from './order-businesses.pipe';

describe('OrderBusinessesPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderBusinessesPipe();
    expect(pipe).toBeTruthy();
  });
});
